interface:
  display_name: "Manage Local Secrets"
  short_description: "Protected local secret operations"
  default_prompt: "Use $manage-local-secrets to manage named development secrets through approved VCSM operations without exposing values."
